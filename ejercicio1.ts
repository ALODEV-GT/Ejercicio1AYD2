
function sortWord(word: string) {
    const letters = word.split("")
    let orderedWord = ""
    const currentIndex: number = 1
    const minLetter

    for (let i: number = 0; i < letters.length; i++) {
        for (let j = currentIndex; j < letters.length; j++) {
            if(letters[i] < letters[j]){

            }    
        }
    }
}

function areAnnagramm(word1: string, word2: string) {
    sortWord("hola")
}

